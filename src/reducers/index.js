

const initialState = {
  id: ' ',
  firstName: '',
  lastName: '',
  email: '',
  username: '',
};

export const userReducer = (state = initialState, action) => {
  switch (action.type){






  default:
  return{...state}
  }
}
